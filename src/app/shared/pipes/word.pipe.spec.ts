/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { WordPipe } from './word.pipe';

describe('Pipe: Worde', () => {
  it('create an instance', () => {
    let pipe = new WordPipe();
    expect(pipe).toBeTruthy();
  });
});
